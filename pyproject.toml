[project]
name = "astral-decomposition"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests"]

[project.optional-dependencies]
claude = ["anthropic"]

[tool.setuptools.packages.find]
where = ["code"]